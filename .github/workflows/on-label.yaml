name: on-label

on:
  pull_request_target:
    types:
      - labeled
    branches:
      - main
      - dev

permissions:
  contents: read
  checks: read
  pull-requests: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      # Check if other builds are complete
      - name: Check if Other Builds Are Complete API
        uses: ./.github/workflows/check-status
        with:
          jobs: docker,test
          sha: ${{ github.event.pull_request.head.sha }}
